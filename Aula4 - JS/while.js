// inicialização
let andar = 5; 

while (andar !== 0) {
    alert (`espera mais um poquinho. Andar atual: ${andar}`);
    andar--;
}
// laço infinito sem a linha 6

alert("pode sair que já chegou");

// no while se preocupar com: inicialização, condição, variável (atualizar a condição)